# Shared - 匿名区块链分享平台

<div align="center">

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Solidity](https://img.shields.io/badge/Solidity-0.8.28-blue)](https://soliditylang.org/)
[![React](https://img.shields.io/badge/React-18-blue)](https://reactjs.org/)
[![Go](https://img.shields.io/badge/Go-1.25-cyan)](https://golang.org/)



![](https://github.com/i0Ek3/shared/blob/main/assets/images/shared.jpg)

</div>



## 📋 目录

- [项目简介](#项目简介)
- [核心特性](#核心特性)
- [技术架构](#技术架构)
- [快速开始](#快速开始)
- [详细部署](#详细部署)
- [使用说明](#使用说明)
- [项目结构](#项目结构)
- [安全提示](#安全提示)
- [常见问题](#常见问题)
- [贡献指南](#贡献指南)
- [许可证](#许可证)



## 🎯 项目简介

Shared 是一个基于区块链和 IPFS 的去中心化内容分享平台。用户可以匿名发布文字、图片、视频内容，所有内容一旦发布将永久保存在区块链上，无法删除或修改。



### 为什么选择 Shared？

- ✅ **完全匿名**：无需注册，无需登录，保护隐私
- ✅ **永久保存**：基于区块链和 IPFS，内容永不丢失
- ✅ **不可删除**：一旦发布，无法撤回，确保言论自由
- ✅ **去中心化**：无中心化服务器，抗审查
- ✅ **开放透明**：所有代码开源，可审计



## 🚀 核心特性

### 1. 匿名发布
- 无需注册账户
- 使用以太坊钱包地址作为匿名标识
- 不收集任何个人信息



### 2. 多媒体支持

- 📝 文字内容
- 🖼️ 图片文件（JPG, PNG, GIF）
- 🎬 视频文件（MP4, WebM）



### 3. 区块链存储

- 智能合约记录内容哈希
- IPFS 存储实际内容
- 完全去中心化



### 4. 现代化界面

- 响应式设计
- 流畅动画效果
- 暗色主题



## 🏗️ 技术架构

```
┌─────────────────────────────────────────────┐
           前端 (React + TypeScript)          
  - 用户界面                                   
  - Web3 集成                                 
  - IPFS 客户端                               
└─────────────────┬───────────────────────────┘
                  │
                  │ ethers.js / HTTP
                  │
┌─────────────────▼───────────────────────────┐
        智能合约 (Solidity 0.8.19)             
  - PostContract                              
  - 存储内容哈希和元数据                         
└─────────────────┬───────────────────────────┘
                  │
                  │ Event Logs
                  │
┌─────────────────▼───────────────────────────┐
          后端服务 (Go + Gin)                  
  - IPFS 上传接口                              
  - 内容检索                                   
  - API 网关                                  
└─────────────────┬───────────────────────────┘
                  │
                  │ IPFS HTTP API
                  │
┌─────────────────▼────────────────────────────┐
              IPFS 存储层                      
  - 去中心化文件存储                             
  - 内容寻址                                    
  - 分布式网络                                  
└──────────────────────────────────────────────┘
```



## ⚡ 快速开始

### 前置要求

- Node.js >= 18.0.0
- Go >= 1.21
- IPFS Desktop 或 IPFS CLI
- MetaMask 钱包
- Git



### 一键安装

```bash
# 克隆仓库
git clone https://github.com/i0Ek3/shared.git
cd shared

# 安装所有依赖
./scripts/install-all.sh

# 启动开发环境
./scripts/dev-start.sh
```

访问 `http://localhost:3000` 即可使用！



## 📦 详细部署

### 1. 安装依赖

#### 安装 Node.js 依赖

```bash
# 智能合约依赖
cd contracts
npm install

# 前端依赖
cd ../frontend
npm install
```

#### 安装 Go 依赖

```bash
cd backend
go mod download
```

#### 安装 IPFS

**macOS:**
```bash
brew install ipfs
```

**Linux:**
```bash
wget https://dist.ipfs.io/go-ipfs/v0.23.0/go-ipfs_v0.23.0_linux-amd64.tar.gz
tar -xvzf go-ipfs_v0.23.0_linux-amd64.tar.gz
cd go-ipfs
sudo bash install.sh
```

**Windows:**
下载并安装 [IPFS Desktop](https://github.com/ipfs/ipfs-desktop/releases)



### 2. 配置环境变量

#### 智能合约环境配置

创建 `contracts/.env`:

```env
# Sepolia 测试网 RPC (可使用 Infura 或 Alchemy)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# 部署账户私钥（注意安全，不要泄露）
PRIVATE_KEY=your_private_key_here

# Etherscan API Key（用于合约验证）
ETHERSCAN_API_KEY=your_etherscan_api_key

# 主网 RPC (生产环境)
MAINNET_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
```

#### 后端环境配置

创建 `backend/.env`:

```env
# IPFS API 地址
IPFS_API_URL=localhost:5001

# 服务端口
PORT=8080

# CORS 允许的源
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
```

#### 前端环境配置

创建 `frontend/.env`:

```env
# 后端 API 地址
VITE_BACKEND_URL=http://localhost:8080

# 网络配置
VITE_CHAIN_ID=31337
VITE_NETWORK_NAME=localhost
```



### 3. 启动 IPFS 节点

```bash
# 初始化 IPFS
ipfs init

# 启动 IPFS 守护进程
ipfs daemon
```

IPFS 将在 `http://localhost:5001` 运行。



### 4. 部署智能合约

#### 开发环境（本地网络）

```bash
cd contracts

# 启动本地 Hardhat 网络
npx hardhat node

# 在新终端部署合约
npx hardhat run scripts/deploy.js --network localhost
```

#### 测试网（Sepolia）

```bash
cd contracts

# 部署到 Sepolia 测试网
npx hardhat run scripts/deploy.js --network sepolia
```

部署完成后，合约地址将自动保存到 `frontend/src/contracts/contract-address.json`。



### 5. 启动后端服务

```bash
cd backend

# 运行 Go 服务
go run main.go
```

后端将在 `http://localhost:8080` 运行。



### 6. 启动前端

```bash
cd frontend

# 开发模式
npm run dev

# 或构建生产版本
npm run build
npm run preview
```

前端将在 `http://localhost:3000` 或 `http://localhost:5173` 运行。



## 📖 使用说明

### 发布内容

1. 打开应用 `http://localhost:3000`
2. 连接 MetaMask 钱包（首次使用需要连接）
3. 在输入框输入文字内容，或点击上传按钮选择图片/视频
4. 点击"永久发布"按钮
5. 确认 MetaMask 交易（支付 Gas 费）
6. 等待交易确认

⚠️ **重要提示**：内容一旦发布将永久保存，无法删除！



### 查看内容

- 最新发布的内容将自动显示在首页
- 内容按时间倒序排列
- 支持查看文字、图片、视频



### 获取测试 ETH

如果在测试网使用，需要获取测试 ETH：

- **Sepolia Faucet**: https://sepoliafaucet.com/
- **Alchemy Faucet**: https://sepoliafaucet.com/



## 📁 项目核心结构

```
Shared
├── backend/
│   ├── Dockerfile
│   ├── go.mod
│   ├── go.sum
│   └── main.go
├── contracts/
│   ├── contracts/
│   ├── hardhat.config.js
│   ├── ignition/
│   ├── package.json
│   ├── README.md
│   ├── scripts/
│   └── test/
├── docker-compose.yml
├── frontend/
│   ├── eslint.config.js
│   ├── index.html
│   ├── package.json
│   ├── postcss.config.js
│   ├── src/
│   ├── tailwind.config.js
│   ├── tsconfig.app.json
│   ├── tsconfig.json
│   ├── tsconfig.node.json
│   └── vite.config.ts
├── package.json
├── README.md
└── scripts/
    ├── dev-start.sh
    └── install-all.sh
```



## ♻️数据流向

```bash
用户输入
   ↓
React 前端 (App.tsx)
   ↓
[文本|文件] → 后端 API (upload函数) → IPFS → 获取哈希
   ↓
前端调用智能合约
   ↓
MetaMask 签名交易
   ↓
PostContract.createPost() 写入区块链
   ↓
触发 PostCreated 事件
   ↓
前端监听事件并刷新列表
   ↓
从 IPFS 加载内容并显示
```



## 🔒 安全提示

### 用户端

1. **私钥安全**
   - 永远不要分享你的私钥
   - 使用硬件钱包（如 Ledger）提高安全性
   - 定期备份钱包

2. **内容审查**
   - 发布前仔细检查内容
   - 一旦发布无法删除
   - 不要发布违法或有害内容

3. **隐私保护**
   - 使用 VPN 或 Tor 增强匿名性
   - 使用新钱包地址发布敏感内容
   - 不要在内容中透露个人信息



### 开发者端

1. **智能合约安全**
   - 合约已通过基础审计
   - 建议专业审计后再部署到主网
   - 使用 Slither、MythX 等工具检测漏洞

2. **API 安全**
   - 实施速率限制
   - 添加内容大小限制
   - 使用 HTTPS 加密传输

3. **IPFS 安全**
   - 定期备份 Pin 的内容
   - 监控存储空间
   - 使用 Pinata 或 Infura 等服务增强可靠性



## ❓ 常见问题

### Q: 为什么需要支付 Gas 费？

A: 发布内容需要在区块链上创建交易，矿工需要 Gas 费来确认交易。这也是防止垃圾内容的一种机制。

### Q: 内容真的无法删除吗？

A: 是的。一旦写入区块链，内容哈希将永久保存。IPFS 内容通过 Pin 机制也会永久保存。

### Q: 如何保证匿名性？

A: 平台不收集任何用户信息。但区块链交易是公开的，建议使用新钱包地址和 VPN/Tor 增强隐私。

### Q: 支持哪些文件格式？

A: 目前支持：
- 图片：JPG, PNG, GIF, WebP
- 视频：MP4, WebM, MOV

### Q: 单个文件大小限制是多少？

A: 建议限制在 100MB 以内。过大的文件会增加上传时间和 IPFS 存储成本。

### Q: 可以在移动端使用吗？

A: 可以！前端是响应式设计，支持移动浏览器。需要安装支持移动端的钱包如 MetaMask Mobile。



## 🤝 贡献指南

我们欢迎所有形式的贡献！

### 如何贡献

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范

- **JavaScript/TypeScript**: 使用 ESLint + Prettier
- **Solidity**: 遵循 Solidity Style Guide
- **Go**: 使用 `gofmt` 和 `golint`

### 提交规范

使用 Conventional Commits 格式：

```
feat: 添加新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```



## 📄 许可证

本项目采用 MIT 许可证 。



## 🙏 致谢

- [Ethereum](https://ethereum.org/) - 区块链平台
- [IPFS](https://ipfs.io/) - 去中心化存储
- [React](https://reactjs.org/) - 前端框架
- [Go](https://golang.org/) - 后端语言
- [Hardhat](https://hardhat.org/) - 智能合约开发工具



## ⚖️ 免责声明

Shared 是一个实验性项目，旨在探索去中心化内容分享的可能性。用户需自行承担使用本平台的风险。开发者不对用户发布的内容负责。请遵守当地法律法规，不要发布非法或有害内容。

---

<div align="center">
  Made with ❤️ by the Shared Team
</div>